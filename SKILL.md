---
name: developer-resume-writer
description: 根据 Git 项目地址、本地项目文件夹或用户现有简历，生成或优化研发简历中的项目简介、项目亮点和项目产出。适用于“根据仓库写项目产出”“把这个项目写进简历”“优化我的简历/项目经历”时使用。
---

# 项目经历写作

核心任务：把项目地址、项目目录或现有简历转成一段讲得清楚、经得起追问、能直接放进简历的项目经历。

## 输入

接受以下任一输入：

- Git 项目地址；
- 本地项目文件夹；
- 项目 README、需求说明或已有项目经历；
- 用户当前简历（Markdown、文本、PDF 转写内容或截图转写内容）；
- 用户补充的职责、成果和目标岗位。

收到项目地址或目录后，直接只读分析项目；不需要额外确认卡。只阅读与理解项目有关的 README、目录结构、核心源码、配置样例和测试代码；不执行项目、不安装依赖、不启动服务。明显的密钥或环境文件不是分析对象。

## 工作流

### 根据项目地址或目录写项目经历

1. 先读 README 与目录结构，识别项目类型、目标用户或业务场景、主要模块和技术栈。
2. 再查看关键入口、核心业务流程、数据/接口模型、异常处理和测试代码，补全 README 中没有说清的部分。
3. 先输出“项目是什么”：用 80–150 字完整说明“服务对象/场景 + 核心功能闭环 + 解决问题 + 技术机制”。项目简介必须让读者知道“这个项目能做什么”，而不只是技术名词堆砌。优先使用：`面向<对象>的<项目类型>，提供<功能 A→功能 B>闭环，解决<问题>；通过<模块/架构/机制>实现<能力>。`
4. 生成简历可用的项目经历：项目名称、项目简介、技术栈和 3–5 条项目亮点/产出。
5. 在用户指定输出目录写入文件；未指定时写入当前工作区的 `resume-deliverables/项目经历.md`。需要时额外生成 `项目简介.md`；用户明确需要 CodeCV 时再生成 `CodeCV-项目经历.md`。

### 根据现有简历给优化方案

1. 先识别目标岗位、已有项目简介、技术栈和每条项目产出；缺少目标岗位时，按简历中最突出的技术方向优化。
2. 对每个项目输出“问题｜优化建议｜改写示例”：检查是否没说核心功能、是否只堆技术词、亮点是否没有结果、是否有无法解释的夸大表述。
3. 给出优化后的项目经历，保留用户提供的真实信息；不把建议改造写成既有产出，也不擅自补数字。
4. 在用户指定输出目录写入 `简历优化建议.md` 和 `优化后项目经历.md`；未指定时写入当前工作区的 `resume-deliverables/`。

同时提供项目源码与现有简历时：简历只用于判断用户做了什么，源码只用于补全项目怎么实现。两者冲突时优先保守写项目事实，并在优化建议中列出待用户确认的职责；不要把源码自动归因成用户贡献。

## 写作规则

- 每条项目亮点严格使用 `- **小标题**｜简介`，整行不超过 200 字；小标题不超过 16 字。
- 先写项目定位和项目简介，再写项目亮点；不要只罗列技术栈。
- 项目简介必须包含“核心功能”：服务对象或场景、至少一个完整功能闭环、解决的问题和技术机制；80–150 字，避免空话。
- 每条亮点内部必须包含“能力点、做法、关键机制和可用结果”，但正文禁止出现“动作：”“方案：”“产出：”这类标签。使用一整句自然表述，例如：`- **任务编排**｜围绕异步任务流转设计状态机与重试机制，使任务状态可追踪并支持失败回补。` 标题应表达能力点，如“任务编排”“权限隔离”，不能写“项目开发”“功能实现”。
- 产出必须回扣项目简介中的核心功能，例如“支持任务状态可追踪和失败重试”，不能凭空写成“性能提升显著”。有用户明确数据才写数字；没有数据时只写“支持、实现、形成、覆盖”等功能性结果。
- 每条亮点都要能在项目源码、README、用户简历或用户补充职责中找到依据；术语、模块和结果要与项目简介一致，确保被追问时能讲清楚。
- 用户明确说明自己做了什么时，可用“负责、实现、优化、推进”等贡献表达。
- 未提供个人职责时，写“项目采用/包含/提供……”的项目事实，并附上可选的“你可确认的贡献方向”；不要把源码自动写成用户个人成果。
- 数据、性能收益和业务效果只使用用户明确提供的数据；没有数据时用技术动作和实现范围表达，不编造数字。
- 根据目标岗位调整重点：后端突出服务设计、接口、数据、稳定性；前端突出交互、性能、工程化；AI/算法突出链路、模型接入、评测和应用效果。
- 默认生成通用 Markdown；CodeCV 只是可选适配。用户要求 CodeCV 时，使用模板：https://www.codecvcv.com/jianlimoban/15simple_versatile，并建议主色 `#2F5CC4`、字号 `17`、行距 `15`、上下边距 `22`。

## 标准输出

根据项目生成时，项目经历必须包含以下内容：

```markdown
## <项目名称>

**项目简介**：<服务对象/场景 + 核心功能闭环 + 解决问题 + 技术机制>

**技术栈**：`技术 A`、`技术 B`

### 项目产出

- **能力点标题**｜<做法>，<关键技术或机制>，<支撑的业务能力或规避的风险>
```

根据现有简历优化时，必须同时交付以下结构：

```markdown
## 优化建议

- **项目简介**｜问题：<原文问题>；建议：<怎么补齐核心功能>
- **项目亮点**｜问题：<原文问题>；建议：<怎么让动作、方案和产出自洽>

## 优化后项目经历

<完整改写稿>
```

交付时先用一句话说明项目是什么或本轮优化重点，再给出生成文件的绝对路径。若项目无法读取或信息不足，直接说明缺什么，并先根据现有材料给出可用草稿与需要补充的贡献方向。
