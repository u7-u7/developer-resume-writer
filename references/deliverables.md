# 文件交付

默认生成 `项目经历.md`，其中包含项目名称、项目简介、技术栈和 3–5 条项目产出。

用户需要更完整的说明时，额外生成 `项目简介.md`，包括项目定位、服务场景、核心能力和技术要点。用户明确需要 CodeCV 时，额外生成 `CodeCV-项目经历.md`。

所有项目产出使用 `- **小标题**｜简介`，单行不超过 200 字。输出目录由用户指定；未指定时使用当前工作区的 `resume-deliverables/`。
